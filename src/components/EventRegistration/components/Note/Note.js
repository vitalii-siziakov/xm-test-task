const Note = () => {
  return (
    <p className='event-registration-note'>
      Don’t have an account? <a className='event-registration-note__link' href='/#'>Create one here</a> and register for the event
    </p>
  );
};

export default Note;
